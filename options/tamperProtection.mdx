---
title: "Tamper Protection"
description: "Tamper Protection safeguards the runtime behavior from being altered by JavaScript pitfalls.\n\n**⚠️ Tamper Protection requires eval and ran in a non-strict mode environment!**\n\n- **This can break your code.**\n- **Due to the security concerns of arbitrary code execution, you must enable this yourself.**\n"
slug: "options/lock.tamperProtection"
---

- Option name: `"lock.tamperProtection"`

- Option values: `true/false/Function`


<Card title="Requires Non-Strict Mode" type="warning">
The obfuscated code will not work properly in Strict Mode.
You can use the [Pack](./pack) option to bypass Strict Mode constraints.
</Card>


<Card title="Requires Eval" type="warning">
The obfuscated code will contain unsafe eval expressions.
The code will not work properly in [environments that have disabled eval](https://developer.mozilla.org/en-US/Web/HTTP/Headers/Content-Security-Policy/script-src#unsafe_eval_expressions).
</Card>

---



#### Input / Output

This example showcases how `Tamper Protection` transforms the code. Try it out by changing the input code and see changes apply in real-time.

```js title="Input.js" lines interactive-mode="obfuscate"
// Input.js
function onTamperDetected() {
  throw new Error('Tampering detected!');
}

fetch('https://jsonplaceholder.typicode.com/users')
  .then((response) => response.json())
  .then((data) => {
    console.log(data); // Array of users with names, usernames, and emails
  });

// Output.js
var _aOKDnP = gIq8mSC();
function gIq8mSC() {
  var _aOKDnP = !1;
  eval('_aOKDnP' + ' = true');
  if (!_aOKDnP) {
    eC3IYJ();
    return {};
  }
  const gIq8mSC = eval('this');
  return gIq8mSC;
}
function n1lfww(gIq8mSC) {
  switch (gIq8mSC) {
    case 'yM1fOd2':
      return _aOKDnP.II421M;
    case 'Kpg8TM':
      return _aOKDnP.dPth4F;
    case 'RMEH1fE':
      return _aOKDnP.fetch;
    case 'KVAQwQu':
      return _aOKDnP.yuGPTN;
    case 'DJssWq':
      return _aOKDnP.xw4gZA;
    case 'pRd42Dc':
      return _aOKDnP.bWQsfJ;
    case 'JNfAvn':
      return _aOKDnP.gmz_15;
    case 'NmM7nTb':
      return _aOKDnP.TSRdBK;
    case 'f4ipc21':
      return _aOKDnP.T82ejTR;
    case 'hRWRtpd':
      return _aOKDnP.uGm5GYV;
    case 'zbvy8A':
      return _aOKDnP.IPC8SrV;
    case 'IzX2Sc6':
      return _aOKDnP.console;
    case 'oggEEq':
      return _aOKDnP.zyp6Ez;
    case 'ZQGO4F':
      return _aOKDnP.Ub0Sgx;
    case 'NCfRuju':
      return _aOKDnP.cTNCgUv;
    case 'xdG3Mc':
      return _aOKDnP.Error;
    case 'qAAAQS':
      return _aOKDnP.U3Jz73H;
    case 'ET4eR1':
      return _aOKDnP.mkfOga;
    case 'xaotWvc':
      return _aOKDnP.Z0erxhz;
    case '58Tgiv':
      return _aOKDnP.I4Jyqy;
    case 'ph4HUvo':
      return _aOKDnP.VvofUr;
    case 'ofGYpgl':
      return _aOKDnP.WGXq2J;
    case 'kL1FQm':
      return _aOKDnP.Object;
    case 'dWbwLNh':
      return _aOKDnP.uY7cES;
    case 'DbVQV0x':
      return _aOKDnP.e6bX9fv;
    case 'RtKbFC':
      return _aOKDnP.KJHUok;
    case '0l3tLCF':
      return _aOKDnP.DpgZWw;
    case 'olLilp':
      return _aOKDnP.mJ5tfZ;
    case 'cr1uaEU':
      return _aOKDnP.z8mEWd;
    case 'BuuPkSO':
      return _aOKDnP.MS0bYFF;
  }
}
var ketUd4 = !1;
function eC3IYJ() {
  if (ketUd4) return;
  ketUd4 = !0;
  zU8Bpu();
}
function oVgFfy() {
  function _aOKDnP(_aOKDnP, gIq8mSC) {
    const ketUd4 = _aOKDnP.length,
      oVgFfy = gIq8mSC.length;
    let zU8Bpu = 0;
    if (oVgFfy > ketUd4) {
      return -1;
    }
    for (let tvqo5Q = 0; tvqo5Q <= ketUd4 - oVgFfy; tvqo5Q++)
      for (let n1lfww = 0; n1lfww < oVgFfy; n1lfww++)
        if (_aOKDnP[tvqo5Q + n1lfww] === gIq8mSC[n1lfww]) {
          zU8Bpu++;
          if (zU8Bpu === oVgFfy) {
            return tvqo5Q;
          }
        } else {
          zU8Bpu = 0;
          break;
        }
    return -1;
  }
  function gIq8mSC(gIq8mSC) {
    if (
      _aOKDnP('' + gIq8mSC, '{ [native code] }') === -1 ||
      typeof n1lfww('kL1FQm').getOwnPropertyDescriptor(gIq8mSC, 'toString') !==
        'undefined'
    ) {
      eC3IYJ();
      return;
    }
    return gIq8mSC;
  }
  var ketUd4 = arguments;
  if (ketUd4.length === 1) {
    return gIq8mSC(ketUd4[0]);
  } else if (ketUd4.length === 2) {
    var oVgFfy = ketUd4[0],
      zU8Bpu = ketUd4[1],
      tvqo5Q = oVgFfy[zU8Bpu];
    tvqo5Q = gIq8mSC(tvqo5Q);
    return tvqo5Q.bind(oVgFfy);
  }
}
(function () {
  function _aOKDnP() {
    try {
      var _aOKDnP = [];
      delete _aOKDnP.length;
    } catch (e) {
      return !0;
    }
    return !1;
  }
  if (_aOKDnP()) {
    eC3IYJ();
    oVgFfy = void 0;
  }
})();
function zU8Bpu() {
  throw new (n1lfww('xdG3Mc'))('Tampering detected!');
}
oVgFfy(n1lfww('RMEH1fE'))('https://jsonplaceholder.typicode.com/users')
  .then((_aOKDnP) => {
    return _aOKDnP.json();
  })
  .then((_aOKDnP) => {
    oVgFfy(n1lfww('IzX2Sc6'), 'log')(_aOKDnP);
  });
```

---



#### Improves Global Concealing

Tamper Protection with `Global Concealing` can detect at runtime if certain global functions have been monkey-patched. The following code exemplifies this:

##### Native function check

```js lines
var _fetch = fetch;
fetch = (...args)=>{
  console.log("Fetch request intercepted!", ...args)
  return _fetch(...args)
}
```

This monkey-patch can be detected by inspecting the `fetch.toString()` value:

```js lines
// Untampered
fetch.toString() // "function fetch() { [native code] }"


// Tampered
fetch.toString()  // "(...args)=>{\n  console.log("Fetch request intercepted!", ...args)\n  return _fetch(...args)\n}"
```

Certain global functions are checked before each invocation to ensure that (1) the arguments cannot be intercepted and (2) their behavior cannot be altered.

##### Stealthy global

A direct `eval` invocation can access the local scope, only if it has not been redefined.

```js lines
let root = {};
eval("root=this"); // Window {window: ...}
```

This method securely obtains the real global object for both the browser and NodeJS. Properties on the global object can still be changed however.

---

#### Disallows Strict Mode

Tamper Protection requires the script to run in non-strict mode. Detection of the script in Strict Mode will be considered tampering. You can control the tampering response using the `lock.countermeasures` option.
      
---


#### Custom Implementation
###### `options.lock.tamperProtection(fnName)`

Control which functions are changed. Returns a `boolean`.
| Parameter | Type | Description |
| --- | --- | --- |
| `fnName` | `string` | The function name proposed receive native check protection. |


---

#### Usage Example

The provided code example will obfuscate the file `input.js` and write the output to a file named `output.js`.

```js title="Usage Example" lines
import JSConfuser from "js-confuser";
import {readFileSync, writeFileSync} from "fs";

// Read input code
const sourceCode = readFileSync("input.js", "utf8");
const options = {
  target: 'browser',
  lock: {
    tamperProtection: true,
    countermeasures: 'onTamperDetected',
  },
  globalConcealing: true,
};

JSConfuser.obfuscate(sourceCode, options).then((result)=>{
  // Write output code
  writeFileSync("output.js", result.code);
}).catch(err=>{
  // Error occurred
  console.error(err);  
});
```

---

##### Enabled In

- [High Preset](/presets/high): No
- [Medium Preset](/presets/medium): No
- [Low Preset](/presets/low): No 



---

##### See Also

- [Countermeasures](/options/countermeasures)

---
title: "Dead Code"
description: "Randomly injects dead code."
slug: "options/deadCode"
---

- Option name: `"deadCode"`

- Option values: `true/false/0-1`

---



### Input / Output

This example showcases how `Dead Code` transforms the code. Try it out by changing the input code and see changes apply in real-time.

```js title="Input.js" lines interactive-mode="obfuscate"
// Input.js
if (true) {
  console.log('This code runs');
} else {
  console.log('This code does not run');
}

// Output.js
if ('bNW7GU' in NqSDCO6) {
  sNB27D();
}
function sNB27D() {
  module.exports = async (
    sNB27D = () => {
      throw new Error('No redacted path provided');
    },
  ) => {
    const NqSDCO6 = new Set(process.argv.slice(2));
    if (!NqSDCO6.has('--version')) {
      if (NqSDCO6.size !== 1) return !1;
      if (!NqSDCO6.has('-v')) return !1;
    }
    await (async (NqSDCO6, xk3gdg) => {
      if (NqSDCO6) return ' (standalone)';
      if (xk3gdg === (await sNB27D())) return ' (local)';
      return '';
    })();
    return !0;
  };
}
function NqSDCO6() {}
{
  if ('lD5Wg7v' in NqSDCO6) {
    xk3gdg();
  }
  function xk3gdg() {
    var sNB27D = function (sNB27D, xk3gdg, PGFuI7) {
        var haKoEb = {};
        if (PGFuI7.length !== sNB27D.length + xk3gdg.length) return !1;
        return NqSDCO6(sNB27D, xk3gdg, PGFuI7, 0, 0, 0, haKoEb);
      },
      NqSDCO6 = function (
        sNB27D,
        xk3gdg,
        lqs76v,
        YlX0grC,
        jzFatFD,
        R32eH_,
        npUWKtP,
      ) {
        var PUtg4c = !1;
        if (R32eH_ >= lqs76v.length) return !0;
        if (npUWKtP['' + YlX0grC + jzFatFD + R32eH_] !== void 0)
          return npUWKtP['' + YlX0grC + jzFatFD + R32eH_];
        if (
          lqs76v[R32eH_] === sNB27D[YlX0grC] &&
          lqs76v[R32eH_] === xk3gdg[jzFatFD]
        ) {
          PUtg4c =
            NqSDCO6(
              sNB27D,
              xk3gdg,
              lqs76v,
              YlX0grC + 1,
              jzFatFD,
              R32eH_ + 1,
              npUWKtP,
            ) ||
            NqSDCO6(
              sNB27D,
              xk3gdg,
              lqs76v,
              YlX0grC,
              jzFatFD + 1,
              R32eH_ + 1,
              npUWKtP,
            );
        } else if (lqs76v[R32eH_] === sNB27D[YlX0grC]) {
          PUtg4c = NqSDCO6(
            sNB27D,
            xk3gdg,
            lqs76v,
            YlX0grC + 1,
            jzFatFD,
            R32eH_ + 1,
            npUWKtP,
          );
        } else if (lqs76v[R32eH_] === xk3gdg[jzFatFD]) {
          PUtg4c = NqSDCO6(
            sNB27D,
            xk3gdg,
            lqs76v,
            YlX0grC,
            jzFatFD + 1,
            R32eH_ + 1,
            npUWKtP,
          );
        }
        npUWKtP['' + YlX0grC + jzFatFD + R32eH_] = PUtg4c;
        return PUtg4c;
      };
    console.log(sNB27D);
  }
  console.log('This code runs');
}
```

---





### Usage Example

The provided code example will obfuscate the file `input.js` and write the output to a file named `output.js`.

```js title="Usage Example" lines
import JSConfuser from "js-confuser";
import {readFileSync, writeFileSync} from "fs";

// Read input code
const sourceCode = readFileSync("input.js", "utf8");
const options = {
  target: 'browser',
  deadCode: true,
};

JSConfuser.obfuscate(sourceCode, options).then((result)=>{
  // Write output code
  writeFileSync("output.js", result.code);
}).catch(err=>{
  // Error occurred
  console.error(err);  
});
```

---

#### Enabled In

- [High Preset](/presets/high): Yes (25%)
- [Medium Preset](/presets/medium): Yes (10%)
- [Low Preset](/presets/low): Yes (5%) 




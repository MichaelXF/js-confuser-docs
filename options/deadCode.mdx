---
title: "Dead Code"
description: "Randomly injects dead code."
slug: "options/deadCode"
---

- Option name: `"deadCode"`

- Option values: `true/false/0-1`

---



#### Input / Output

This example showcases how `Dead Code` transforms the code. Try it out by changing the input code and see changes apply in real-time.

```js title="Input.js" lines interactive-mode="obfuscate"
// Input.js
if (true) {
  console.log('This code runs');
} else {
  console.log('This code does not run');
}

// Output.js
if ('ibB17S' in XkxoVOK) {
  XGlB68U();
}
function XGlB68U() {
  var XGlB68U = function (XGlB68U) {
    var XkxoVOK = 0,
      Veyho3 = {},
      tMDcbf = 0,
      leIfF5 = 0,
      E62CiP = 0,
      harHny = XGlB68U.length;
    for (var f7oyuS = 0; f7oyuS < harHny; f7oyuS++) {
      Veyho3 = {};
      tMDcbf = 0;
      leIfF5 = 1;
      for (var nlyHkGS = f7oyuS + 1; nlyHkGS < harHny; nlyHkGS++) {
        if (
          XGlB68U[f7oyuS].x === XGlB68U[nlyHkGS].x &&
          XGlB68U[f7oyuS].y === XGlB68U[nlyHkGS].y
        ) {
          leIfF5++;
          continue;
        }
        XGlB68U[f7oyuS].y === XGlB68U[nlyHkGS].y
          ? (E62CiP = Number.MAX_SAFE_INTEGER)
          : (E62CiP =
              (XGlB68U[f7oyuS].x - XGlB68U[nlyHkGS].x) /
              (XGlB68U[f7oyuS].y - XGlB68U[nlyHkGS].y));
        if (!Veyho3[E62CiP]) Veyho3[E62CiP] = 0;
        Veyho3[E62CiP]++;
        tMDcbf = Math.max(tMDcbf, Veyho3[E62CiP]);
      }
      tMDcbf += leIfF5;
      XkxoVOK = Math.max(XkxoVOK, tMDcbf);
    }
    return XkxoVOK;
  };
  console.log(XGlB68U);
}
function XkxoVOK() {}
{
  if ('FOIH9A' in XkxoVOK) {
    Veyho3();
  }
  function Veyho3() {
    function XGlB68U() {}
    var XkxoVOK = function (XkxoVOK, Veyho3) {
      var QtLFPFG = 0,
        Vzq9qL = 0,
        R1SaU4 = new XGlB68U(0),
        NIVWJhA = R1SaU4,
        Hga12no = XkxoVOK,
        Eyp8DP = Veyho3;
      while (Hga12no !== null || Eyp8DP !== null) {
        Vzq9qL =
          (Hga12no ? Hga12no.val : 0) + (Eyp8DP ? Eyp8DP.val : 0) + QtLFPFG;
        QtLFPFG = Math.floor(Vzq9qL / 10);
        NIVWJhA.next = new XGlB68U(Vzq9qL % 10);
        NIVWJhA = NIVWJhA.next;
        Hga12no = Hga12no ? Hga12no.next : null;
        Eyp8DP = Eyp8DP ? Eyp8DP.next : null;
      }
      if (QtLFPFG) NIVWJhA.next = new XGlB68U(QtLFPFG);
      return R1SaU4.next;
    };
    console.log(XkxoVOK);
  }
  console.log('This code runs');
}
```

---





#### Usage Example

The provided code example will obfuscate the file `input.js` and write the output to a file named `output.js`.

```js title="Usage Example" lines
import JSConfuser from "js-confuser";
import {readFileSync, writeFileSync} from "fs";

// Read input code
const sourceCode = readFileSync("input.js", "utf8");
const options = {
  target: 'browser',
  deadCode: true,
};

JSConfuser.obfuscate(sourceCode, options).then((result)=>{
  // Write output code
  writeFileSync("output.js", result.code);
}).catch(err=>{
  // Error occurred
  console.error(err);  
});
```

---

##### Enabled In

- [High Preset](/presets/high): Yes (25%)
- [Medium Preset](/presets/medium): Yes (10%)
- [Low Preset](/presets/low): Yes (5%) 




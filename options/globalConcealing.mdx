---
title: "Global Concealing"
description: "Global Concealing hides global variables being accessed."
slug: "options/globalConcealing"
---

- Option name: `"globalConcealing"`

- Option values: `true/false/0-1/Function`

---



#### Input / Output

This example showcases how `Global Concealing` transforms the code. Try it out by changing the input code and see changes apply in real-time.

```js title="Input.js" lines interactive-mode="obfuscate"
// Input.js
console.log('Hello World');

// Output.js
var YYG3XS = o7x7GGS();
function o7x7GGS() {
  var YYG3XS = [
      function () {
        return globalThis;
      },
      function () {
        return global;
      },
      function () {
        return window;
      },
      function () {
        return new Function('return this')();
      },
    ],
    o7x7GGS,
    lt_psj = [];
  try {
    o7x7GGS = Object;
    lt_psj.push(''.__proto__.constructor.name);
  } catch (e) {}
  tDscwqW: for (var PvzZNcW = 0; PvzZNcW < YYG3XS.length; PvzZNcW++)
    try {
      o7x7GGS = YYG3XS[PvzZNcW]();
      for (var I1ZhGKi = 0; I1ZhGKi < lt_psj.length; I1ZhGKi++)
        if (typeof o7x7GGS[lt_psj[I1ZhGKi]] === 'undefined') continue tDscwqW;
      return o7x7GGS;
    } catch (e) {}
  return o7x7GGS || this;
}
function lt_psj(o7x7GGS) {
  switch (o7x7GGS) {
    case 'ibtndB':
      return YYG3XS.o6RVSM;
    case 'MoDjev':
      return YYG3XS.uvcg1SO;
    case 'BNDgXy':
      return YYG3XS.console;
    case 'AMyvNJ':
      return YYG3XS.oonw9J;
    case 'Ipoeytk':
      return YYG3XS.dg7w0V;
    case 'r51nl6':
      return YYG3XS.aO8aM6;
    case 'hUGtsma':
      return YYG3XS.tVAocqo;
    case 'd7KYQ2':
      return YYG3XS.yuLsyf0;
    case 'pykOJU':
      return YYG3XS.EXo2Sm9;
    case 'WglPEl6':
      return YYG3XS.LJvMTS;
    case 'VF6SIg1':
      return YYG3XS.EEZJmF;
    case 'fZBz7CA':
      return YYG3XS.l3KwvGW;
    case 'iWNTItb':
      return YYG3XS.z2gQB5;
    case 'dl8vTA':
      return YYG3XS.klYxSjZ;
    case 'CxCiFIR':
      return YYG3XS.BZhnmVi;
    case 'lAUTMk':
      return YYG3XS.Y_WKu2;
    case 'ZMWej4y':
      return YYG3XS.g_P6w9;
    case 'UlkiFJ':
      return YYG3XS.MB2nWPC;
    case 'CLCmLJb':
      return YYG3XS.PLE94x;
    case '6BMWmH':
      return YYG3XS.fuOifC;
    case 'AQOOkr':
      return YYG3XS.BdvEYo;
    case '4B0EfS1':
      return YYG3XS.lIcuWv;
    case 'tsXoPK':
      return YYG3XS.vmpTR4;
    case 'GYOhJv':
      return YYG3XS.Y5Wnc4;
  }
}
lt_psj('BNDgXy').log('Hello World');
```

---




#### Custom Implementation
###### `options.globalConcealing(name)`

Control which globals are changed. Returns a `boolean`.
| Parameter | Type | Description |
| --- | --- | --- |
| `name` | `string` | The global variable proposed to be concealed. |


---

#### Usage Example

The provided code example will obfuscate the file `input.js` and write the output to a file named `output.js`.

```js title="Usage Example" lines
import JSConfuser from "js-confuser";
import {readFileSync, writeFileSync} from "fs";

// Read input code
const sourceCode = readFileSync("input.js", "utf8");
const options = {
  target: 'browser',
  globalConcealing: true,
};

JSConfuser.obfuscate(sourceCode, options).then((result)=>{
  // Write output code
  writeFileSync("output.js", result.code);
}).catch(err=>{
  // Error occurred
  console.error(err);  
});
```

---

##### Enabled In

- [High Preset](/presets/high): Yes
- [Medium Preset](/presets/medium): Yes
- [Low Preset](/presets/low): No 




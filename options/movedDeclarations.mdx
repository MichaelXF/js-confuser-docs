---
title: "Moved Declarations"
description: "Moves variable declarations to the top of the context."
slug: "options/movedDeclarations"
---

- Option name: `"movedDeclarations"`

- Option values: `true/false/0-1`

---



#### Input / Output

This example showcases how `Moved Declarations` transforms the code. Try it out by changing the input code and see changes apply in real-time.

```js title="Input.js" lines interactive-mode="obfuscate"
// Input.js
function getAreaOfCircle(radius) {
  var pi = Math.PI;
  var radiusSquared = Math.pow(radius, 2);
  var area = pi * radiusSquared;

  return area;
}

console.log(getAreaOfCircle(3)); // 28.274333882308138

// Output.js
function ef39wu(ef39wu, GsHuZP, UySyuc4, xMp5G2A) {
  GsHuZP = Math.PI;
  UySyuc4 = Math.pow(ef39wu, 2);
  xMp5G2A = GsHuZP * UySyuc4;
  return xMp5G2A;
}
console.log(ef39wu(3));
```

---





#### Usage Example

The provided code example will obfuscate the file `input.js` and write the output to a file named `output.js`.

```js title="Usage Example" lines
import JSConfuser from "js-confuser";
import {readFileSync, writeFileSync} from "fs";

// Read input code
const sourceCode = readFileSync("input.js", "utf8");
const options = {
  target: 'browser',
  movedDeclarations: true,
};

JSConfuser.obfuscate(sourceCode, options).then((result)=>{
  // Write output code
  writeFileSync("output.js", result.code);
}).catch(err=>{
  // Error occurred
  console.error(err);  
});
```

---

##### Enabled In

- [High Preset](/presets/high): Yes
- [Medium Preset](/presets/medium): Yes
- [Low Preset](/presets/low): Yes 




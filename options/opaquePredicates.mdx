---
title: "Opaque Predicates"
description: "An Opaque Predicate is a predicate(true/false) that is evaluated at runtime, this can confuse reverse engineers from understanding your code."
slug: "options/opaquePredicates"
---

- Option name: `"opaquePredicates"`

- Option values: `true/false/0-1`

---



#### Input / Output

This example showcases how `Opaque Predicates` transforms the code. Try it out by changing the input code and see changes apply in real-time.

```js title="Input.js" lines interactive-mode="obfuscate"
// Input.js
if(true) {
        console.log("This code runs");
      } else {
        console.log("This code does not run");
      }

// Output.js
function YVbdPh() {}
!("yzM296" in YVbdPh) && !0 ? console.log("This code runs") : console.log("This code does not run");
```

---





#### Usage Example

The provided code example will obfuscate the file `input.js` and write the output to a file named `output.js`.

```js title="Usage Example" lines
import JSConfuser from "js-confuser";
import {readFileSync, writeFileSync} from "fs";

// Read input code
const sourceCode = readFileSync("input.js", "utf8");
const options = {
  target: 'browser',
  opaquePredicates: true,
};

JSConfuser.obfuscate(sourceCode, options).then((result)=>{
  // Write output code
  writeFileSync("output.js", result.code);
}).catch(err=>{
  // Error occurred
  console.error(err);  
});
```

---

##### Enabled In

- [High Preset](/presets/high): Yes (75%)
- [Medium Preset](/presets/medium): No
- [Low Preset](/presets/low): No 




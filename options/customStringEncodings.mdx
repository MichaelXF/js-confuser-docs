---
title: "Custom String Encodings"
description: "Custom String Encodings allows you to define your own string encoding/decoding functions."
slug: "options/customStringEncodings"
---

- Option name: `"customStringEncodings"`

- Option values: `true/false/0-1`

---



#### Input / Output

This example showcases how `Custom String Encodings` transforms the code. Try it out by changing the input code and see changes apply in real-time.

```js title="Input.js" lines interactive-mode="obfuscate"
// Input.js
// Base64 Encoding Demo

var str = 'Hello, World!';
console.log(str); // "Hello, World!"

// Output.js
function sY32OiU(sY32OiU) {
  return atob(sY32OiU);
}
function zOvQmA(zOvQmA) {
  if (typeof _q5Krz[zOvQmA] === 'undefined') {
    return (_q5Krz[zOvQmA] = sY32OiU(Lp04FQ[zOvQmA]));
  }
  return _q5Krz[zOvQmA];
}
var _q5Krz = {},
  Lp04FQ = [
    'OHl4a1VndEZGbzg2bGJtOQ==',
    'eGVhbWJJRzlkNHl3a1VZRQ==',
    'RDA2bFB1Q3k1eTBJbnFwZThoUUFuZW5FcGJKb2h0VkR6NE1RZ0dSa08=',
    'R0M3ZVlz',
    'Z0tXVHBvZWJxSW5HYjJac1dMUGpkSGNqa0EwMnYzTmdYV1VuYkd3ODRO',
    'NGFwUGhyWjFFQUlzRVRpZExsdnNib09yaXk=',
    'QXNrMmpkbUhUU0VYYW9FNGtDM2E=',
    'VjE0aTVJT1hMczhKTVdOb1o4Q3hwWGhCY095MkJlUkhETkpi',
    'cWRkQ1VIZ2dVNERORUxjRFdqS0tMZw==',
    'WFRCSE9nZG0xMlYxT1BuTEM4Uw==',
    'U1o2Q3VMTm4zTUF5V1RPNHc2cnhra2tzS0tOT3NX',
    'ZW1hWjV2NnJid1hBY2plNmpGTA==',
    'STFsa3dpUnBZdlpBR2VLVkp6dkV3N1E1TG5uemM=',
    'ckRJVUw5OElJNGlZVjVFUkxJNmhGelQzckhMcjR2cUl5eDhWRHZodzZYNGI=',
    'ZE1FYm5wMGNmaWYwUVlDOFJxaFozTFFGRUhLRkFqRTdmTA==',
    'TWhUV1pPQURnVk00bG05VkdJTFpSSjN6TmdkTEk1RmdxN1o=',
    'TlhLaXh2VnF4TE5PS0I3bjgyaXB1Tzlha3VrSQ==',
    'R25wdTBLRU0xUUg2RTJjaDZHSFdQUmpmYnM=',
    'QnV4ZVo5dVpoQllFblk0QWdCZGVGUWJlRVpuclNFdmFjR0kzVUw4TENJQQ==',
    'Ymc3Qm1ncHBOb1Z6OHc0QzZpb3REdUdXWlhncjdsb2tLVHU3N2tyS3FwcGFFeURQMw==',
    'T1B2QzJMWXRvY0I4RjVKY004M3l2',
    'dnA5REdpUjk0d3RDYXZOSk4=',
    'RGFsQzI=',
    'OEZQVlFQRVRGUEIzTDhqYXg5YQ==',
    'eWtSRTNrNQ==',
    'RWlYU25PdTN1TG1IUlFCZTdLZUNoTFZqRmhGaHl2dVg0ZUJWTDd0SnhXNzdXaFg=',
    'TUFpTmZzSVNsdlNka3JBUEZrNXFZeExoQUVzdENNeXhFVA==',
    'cFRubW1GMTRoM29pbWozQmRqWmE5SFVNNA==',
    'SFA2YjJPOGpZS25EaXN5cDY0QTFremFKa2pMaklEWkJWWmx5WA==',
    'U0VmSkl1QmFSaVJuN2l5QURJNDNRQVZOaUU5RHBnNzNWR0dpWWZZUw==',
    'SkVTb0JUUzJUVDNnVFV1UVR5d1ZrSmlZN2JDRzIyZjRSQ2V4MGhjUQ==',
    'MEdFMnowcWNOWGxlZndLN2Y=',
    'bXZLbTF6eU1QbWJoeVNNTU1sc2ZS',
    'NVNpREx4SWc5TFN0',
    'ZEluSjNRSnNaQTBqR2NsY0VpNFA=',
    'eUNHaEFtdXF5UWZiSzZwNkR2dFNLUFBQVlpHYQ==',
    'bk9rbHJBZzZoTWJZbXBNT0N2V0tGa3hkZEJhcDZBT2NUbzRueg==',
    'TEFPMGVTeTE1c3pKRE9ZU2xVN01Q',
    'ZkhPeHdwMmt4ODQ3R0VYY2YyOHV1NG1UYm5wUTFNRlllU2tHSnZJNWwz',
    'ZGFoZ1JY',
    'bWRGMEN4WW9WOVpkVGh1UTRMVVFOd3picQ==',
    'ZFZSdmk=',
    'UFBacXZRam1hbW5XU0t4alUwQzhMRUJ3WmV4bHNDaXNBY0d1Qw==',
    'bEJrZjhpVXp3c0JaMGl1ZW1qZXhCak1FbkNvUURpbWNZeEJQcGJLczB0R1FSTmM3cw==',
    'SlRwcm9UWXpTUXRiMWh6N29xZEo2',
    'Z1ptMEVzeTh0TE1HN04zMU1sc1kwcWYxQVJrV1FlVFJXRFZ4VzVyYVg=',
    'UVdzYzVHbXNMTWl5TGVLUW5JVU1Hd3FVanA2aGhTQ1hRUHg0NTdMTWZyaVo1c1Y=',
    'OFcydXF6c3MyYVVXd0FSMEw4',
    'bVF1TkhKMXlBWFRRWnJpZFNTaGROUFU=',
    'ZU9oZ2JBVmdraHE5WUdhOEx1cE1PVFgwckloQk1hQ2FVSmNu',
    'cGtEVlF0R3M2QmwxcjUyZE5WUUhSMWpwUng0RXJkakd3bzNiNlJkTg==',
    'RmNlVjVqNVM=',
    'MEdHcG9QTVVoWWZJS1dtN1JJd0FENEVWUklQektNQlE=',
    'WWx6WHZqMzF0N1FzMXJyQ3J2VEE=',
    'SVBOVnNpbm5PUDdZeQ==',
    'UHFGckdx',
    'ZXJxQko1aFNtd0tRTDNobmJtNG5KcVR5QUtRbHZQTkNyY0VaTUM=',
    'TW5BWlZnaVNIN3FZUVNoRWNSelF5ZWVaTlBrRndrTA==',
    'cVVjMEJ0eTh1TWQ=',
    'RlR5ejQxVmV2MzZSUno0ZEQ=',
    'THdHVHMzQk9oWlphRmdJ',
    'dFVqNGNvdXl6SXhVMkhOblI1dDF4UUpqcFFQd3lUczVFZGR5a21yRGNkWA==',
    'UkJCOHVpelg0ckp5RA==',
    'dXl6eWVzbkZ3Rjc2dkVFOFF0Tlo5Z21DRTVnNDlSczhEU2VPUjQ=',
    'U2ppbHpRT2w4Vkd5M2R3VTNjRkQ2ckN1NGl2ajh2bjBL',
    'ZXV0dUhJNDEybkhMeDRRcDF2MXdWN1ZSYmxzN0h1OTNZRWE2MQ==',
    'ak8weHIxQkRmdkZobGRUckQ3RkNCZTlxeWtJNDNYc0dCVE93aExUOXhLa21hcXlX',
    'TkJxaFJRTTdKZ0JFc1c5ZzMxME42clN0UjJqVTlpYnV2TDZuUGQ5Z0FRZms3Nw==',
    'UWxOVDQ=',
    'VmFGSDdzS1hEY0ZZZw==',
    'WDlDTksxVQ==',
    'bDdZWU83ZzhCRDlpWEt5bEdtYU11Q3JpQ3h4VHJPU0UyQ3YyUA==',
    'RUk5aURkckNJR0s=',
    'eVY3bTQ=',
    'SEV3TXhmeEptalpPeUJuMWM=',
    'Vks0cDVlR2FYZXRFc0ZBOG5pMWphcmlmcWNWWlgyNDhLOHJocmlkUXJNN3Y=',
    'NmhGaFdMRXVQYkU=',
    'bmV2OWtkdWtKZXFzUm1CUlppQU50YkJ4Wm9XaA==',
    'cnlNTkRNRERVTlJIOUUzU3d6YTN3aWMxVjZyMnZXTGc5',
    'aHA3RmNjangycExoMGE3cEEwcTVhMTg2MUQwZkxTak4wTkxlaXluVGg4Q0J3',
    'elRRVTE=',
    'dnRtOFNyUEdQaHhEMEFNQkV5cXI3dlZaMXlHaUI1NzNkTUh1',
    'UDFLcDJ6b0FxMmdReDFZdnhabnNmaDdyOGNCd0Qx',
    'TlFMMFhaOGg3VERwbDNrbHE=',
    'azFiTEoxd0JQcU5MTm1qRUNXVnJOVEZpaWdZeW8=',
    'b1VzTWlGYUhuQlczaWpPS24zMTZQ',
    'd0tzV0pwRjNNcUZFVm1WUTFadkxxa0VnbFAzZXE=',
    'dUpXWk5CY2dOeVJUUXcwVWhXRnJXTE9PRGZ5U1J0QnY=',
    'MUJYY25GSEJBVVZYWA==',
    'UVBXWTI5S0VRZ3BUWnVGR2gx',
    'azJzM0N1M0JXQXEzYk5VV2o5Z00=',
    'bFhoSVp5dk1F',
    'WXpqQjFqTktzdzE3QkhnQkJrVnhlbE5MZ3Yxa01PbFZXbE5JTHZqcHZZOUpx',
    'NUxYWktrVjFUeGVmb21XUXh4Tko=',
    'VDQyMFF3dFNXQWRCbHNuSWxOM2tWWFNRdg==',
    'N3JyU2J0azM2TzVaeUFVWGs0',
    'U3lsdGhW',
    'Q0x1RzViZkZhWGpkMA==',
    'TDlkbFk=',
    'UjViMjhDeGtJcDIyWHlDTVpPS0xPYW1sTmZvV1hKVXZMYzZSMWgw',
    'cVVFSDhTSEVC',
    'NmFzQjYzVHZG',
    'azFKY1hvc3VBbWlkV2FnZnI1VHN0eVlMbmc1SThEanZNVw==',
    'Wk9XZm8wQjZjTGJhOTd1bDdiMUE2WlZIc05WclR6Rg==',
    'VEpwb05HbzRVdDdEN3V3Y0w5S1RrTUZyVFNpMUZmSUZVWVhPY0xaajJXREhDR28=',
    'SGVsbG8sIFdvcmxkIQ==',
    'bG9n',
  ];
function jZfc7h() {
  var sY32OiU = [
      function () {
        return globalThis;
      },
      function () {
        return global;
      },
      function () {
        return window;
      },
      function () {
        return new Function('return this')();
      },
    ],
    zOvQmA,
    _q5Krz = [];
  try {
    zOvQmA = Object;
    _q5Krz.push(''.__proto__.constructor.name);
  } catch (e) {}
  _j1ivK: for (var Lp04FQ = 0; Lp04FQ < sY32OiU.length; Lp04FQ++)
    try {
      zOvQmA = sY32OiU[Lp04FQ]();
      for (var jZfc7h = 0; jZfc7h < _q5Krz.length; jZfc7h++)
        if (typeof zOvQmA[_q5Krz[jZfc7h]] === 'undefined') continue _j1ivK;
      return zOvQmA;
    } catch (e) {}
  return zOvQmA || this;
}
var yfZpZl_ = jZfc7h() || {},
  A91YOu = yfZpZl_.TextDecoder,
  MGGVU6 = yfZpZl_.Uint8Array,
  Q24Uvc = yfZpZl_.Buffer,
  mrqzF1 = yfZpZl_.String || String,
  Dt9cJf = yfZpZl_.Array || Array,
  YKYsT5y = (function () {
    var sY32OiU = new Dt9cJf(128),
      zOvQmA = mrqzF1.fromCodePoint || mrqzF1.fromCharCode,
      _q5Krz = [];
    return function (Lp04FQ) {
      var jZfc7h,
        yfZpZl_,
        A91YOu = Lp04FQ.length;
      _q5Krz.length = 0;
      for (var MGGVU6 = 0; MGGVU6 < A91YOu; ) {
        yfZpZl_ = Lp04FQ[MGGVU6++];
        yfZpZl_ <= 0x7f
          ? (jZfc7h = yfZpZl_)
          : yfZpZl_ <= 0xdf
            ? (jZfc7h = ((yfZpZl_ & 0x1f) << 6) | (Lp04FQ[MGGVU6++] & 0x3f))
            : yfZpZl_ <= 0xef
              ? (jZfc7h =
                  ((yfZpZl_ & 0x0f) << 12) |
                  ((Lp04FQ[MGGVU6++] & 0x3f) << 6) |
                  (Lp04FQ[MGGVU6++] & 0x3f))
              : mrqzF1.fromCodePoint
                ? (jZfc7h =
                    ((yfZpZl_ & 0x07) << 18) |
                    ((Lp04FQ[MGGVU6++] & 0x3f) << 12) |
                    ((Lp04FQ[MGGVU6++] & 0x3f) << 6) |
                    (Lp04FQ[MGGVU6++] & 0x3f))
                : ((jZfc7h = 63), (MGGVU6 += 3));
        _q5Krz.push(sY32OiU[jZfc7h] || (sY32OiU[jZfc7h] = zOvQmA(jZfc7h)));
      }
      return _q5Krz.join('');
    };
  })();
function r88yeo(sY32OiU) {
  return typeof A91YOu !== 'undefined' && A91YOu
    ? new A91YOu().decode(new MGGVU6(sY32OiU))
    : typeof Q24Uvc !== 'undefined' && Q24Uvc
      ? Q24Uvc.from(sY32OiU).toString('utf-8')
      : YKYsT5y(sY32OiU);
}
var MpsGdYD = zOvQmA(105);
console[zOvQmA(106)](MpsGdYD);
```

---



      #### Custom String Encoding API

      The Custom String Encoding API allows you to define your own string encoding/decoding functions. These encodings will be randomly inserted throughout the code.
      
      ```js title="Options.js" lines
      module.exports = {
        target: "node",

        // Should be enabled
        stringConcealing: true,

        // Simple Base64 Encoding
        customStringEncodings: [
          {
            // This template decoder function will be inserted into the code
            code: `
                  function {fnName}(str){
                    return atob(str);
                  }`,

            // Tells the obfuscator how to encode the string
            encode: (str) => btoa(str),
          },
        ],
      };
      ```

      ---

      The properties of the type `Custom String Encoding` are:

      | Property | Type | Description |
      | --- | --- | --- |
      | `code` | `string` | Template decoder code that must contain '{fnName}'. |
      | `encode` | `Function` | Encoding algorithm. |
      | `decode?` | `Function` | Decoding algorithm. (Optional) |
      | `identity?` | `string` | Distinguishes multiple encodings. (Optional) |

      - The template `code` should contain the string `{fnName}`, which the obfuscator can interpolate with the function name.

      - The functions `encode` and `decode` have the type: `(strValue: string) => string`.

      - The function `decode` is optional. If provided, the obfuscator will validate each string to ensure it can be decoded. If the string cannot be decoded, the obfuscator will ignore the string.

      ---
     
      #### Advanced Randomized Encoding

      The following example implements a custom Base64 encoding that uses a shuffled charset to encode and decode strings.

      - This encoding algorithm is instantiated multiple times, each with a different shuffled charset. This makes it difficult to reverse-engineer the encoding algorithm. 
      
      ```js title="Options.js" lines
      const { Template } = require("js-confuser");
      const { stringLiteral } = require("@babel/types");

      function shuffle(array) {
        // Fisher-Yates shuffle
        let currentIndex = array.length,
          randomIndex;
        while (currentIndex !== 0) {
          randomIndex = Math.floor(Math.random() * currentIndex);
          currentIndex--;

          [array[currentIndex], array[randomIndex]] = [
            array[randomIndex],
            array[currentIndex],
          ];
        }
        return array;
      }

      function createCustomStringEncoding() {
        function encode(input, charset) {
          const inputBuffer = new TextEncoder().encode(input);
          let output = "";

          for (let i = 0; i < inputBuffer.length; i += 3) {
            const chunk = [inputBuffer[i], inputBuffer[i + 1], inputBuffer[i + 2]];

            const binary = (chunk[0] << 16) | (chunk[1] << 8) | (chunk[2] || 0);

            output += charset[(binary >> 18) & 0x3f];
            output += charset[(binary >> 12) & 0x3f];
            output +=
              typeof chunk[1] !== "undefined" ? charset[(binary >> 6) & 0x3f] : "=";
            output += typeof chunk[2] !== "undefined" ? charset[binary & 0x3f] : "=";
          }

          return output;
        }

        const customCharset =
          "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/";
        const shuffledCharset = shuffle(customCharset.split("")).join("");

        return {
          code: new Template(`
            // Creates a reverse lookup table from the given charset
            function createReverseCharset(charset) {
              if (charset.length !== 64) {
                throw new Error("Charset must be exactly 64 characters long.");
              }
              const reverseCharset = {};
              for (let i = 0; i < charset.length; i++) {
                reverseCharset[charset[i]] = i;
              }
              return reverseCharset;
            }

            // Base64 decode using the shuffled charset
            function decode(input, charset) {
              const reverseCharset = createReverseCharset(charset);
              const cleanedInput = input.replace(/=+$/, '');  // Remove padding

              const byteArray = [];
              let buffer = 0;
              let bitsCollected = 0;

              for (let i = 0; i < cleanedInput.length; i++) {
                buffer = (buffer << 6) | reverseCharset[cleanedInput[i]];
                bitsCollected += 6;

                if (bitsCollected >= 8) {
                  bitsCollected -= 8;
                  byteArray.push((buffer >> bitsCollected) & 0xFF);
                }
              }

              // Convert to string, ensuring no extra characters
              return new TextDecoder().decode(Uint8Array.from(byteArray));
            }

            var {fnName} = (str) => decode(str, {shuffledCharset});
            `).setDefaultVariables({
            // This simply inserts 'shuffledCharset' (with proper escaping)
            shuffledCharset: stringLiteral(shuffledCharset),
          }),
          encode: (input) => {
            // Encode the string
            return encode(input, shuffledCharset);
          },

          // Identity key to help distinguish between different variants
          identity: shuffledCharset,
        };
      }

      module.exports = {
        target: "node",
        stringConcealing: true,
        customStringEncodings: [createCustomStringEncoding],
      };
      ```
      
---



#### Usage Example

The provided code example will obfuscate the file `input.js` and write the output to a file named `output.js`.

```js title="Usage Example" lines
import JSConfuser from "js-confuser";
import {readFileSync, writeFileSync} from "fs";

// Read input code
const sourceCode = readFileSync("input.js", "utf8");
const options = {
  target: 'browser',
  customStringEncodings: [
    {
      code: `
            function {fnName}(str){
              return atob(str)
            }
            `,
      encode: function(str) {
              return btoa(str);
            },
      decode: function(str) {
              return atob(str);
            },
    },
  ],
  stringConcealing: true,
};

JSConfuser.obfuscate(sourceCode, options).then((result)=>{
  // Write output code
  writeFileSync("output.js", result.code);
}).catch(err=>{
  // Error occurred
  console.error(err);  
});
```

---

##### Enabled In

- [High Preset](/presets/high): No
- [Medium Preset](/presets/medium): No
- [Low Preset](/presets/low): No 




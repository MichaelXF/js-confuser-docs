---
title: "Custom String Encodings"
description: "Custom String Encodings allows you to define your own string encoding/decoding functions."
slug: "options/customStringEncodings"
---

- Option name: `"customStringEncodings"`

- Option values: `true/false/0-1`

---



### Input / Output

This example showcases how `Custom String Encodings` transforms the code. Try it out by changing the input code and see changes apply in real-time.

```js title="Input.js" lines interactive-mode="obfuscate"
// Input.js
// Base64 Encoding Demo

var str = 'Hello, World!';
console.log(str); // "Hello, World!"

// Output.js
function TOphBlR(TOphBlR) {
  return atob(TOphBlR);
}
function AV2ARuN(AV2ARuN) {
  if (typeof ygYSIfw[AV2ARuN] === 'undefined') {
    return (ygYSIfw[AV2ARuN] = TOphBlR(TgSNtHY[AV2ARuN]));
  }
  return ygYSIfw[AV2ARuN];
}
var ygYSIfw = {},
  TgSNtHY = [
    'OHR4Z01RQmduUENwS0RxdGVlRUtsWTQ=',
    'eTdvMTMxYXlH',
    'UTdqVWtMQUp1RnpBaUVQSw==',
    'ZGFDdUhqWXE=',
    'TGZEVjV4bmJPYXN3TFQ0WmJVT09oWTlFVzdZ',
    'djRoV2ZMNG4xOHJqazFwNDBiWGNac0hLdVZzZjlmU0VrSHVsSGZwaDQ4bHprV3lL',
    'S2QxMVBLcHQ0bEdBVGJCZEMzUlQ5TTVJNjIzQmN4TWFRbkV5UkpqWnNzaWdFMQ==',
    'dUphRTlGM2FEVTJlbWc0',
    'WHFGR0YybE83Y09QcmtSc2N6bTJxc2c5dkV2UE9UQTZ6',
    'QjJldE5hYlhTS05MS05lOXh3YlV2TldDZ0hXbkF1V3Fk',
    'RmNHMVpJVVJFd0M1UjFyU0pTSjlIaURhOUZOY1pBbjVpUjBC',
    'SVJhdWxIUWFIcDloY09qeg==',
    'QzdQTmZVdlMxeUdPWW9DSXBIQ1Bnck1OZTJLRw==',
    'a1RGNW40UHY2eGJJSmZYMHg3cFRyZ1k4cjdPQjc0QjNZZm5jZ1Jtcg==',
    'NG5salVLM3gzTXNYVEZxQXpxR3pvc3Y5emV3RlFRTzB3ZWljRkpx',
    'QkxYc1ljRmxXdGlNaFpxQw==',
    'cnIzYUZPV1YxczFDdHBGSUY=',
    'aXNmU2NPS1IyeFNXTHRJdWdsd0hkZmhaTGtIYlFtOA==',
    'SGxJQkhFRFJ5',
    'Qk1rS0tq',
    'ZDM4eGRPOGZsUWNmWDU4bkZHUWtCRk53cDI2cWdFYUFTNDVkSWZNWmJkNG5RVFA=',
    'c29WbHpOblRTdWxwVEFoOHpkUTU1VWc=',
    'U2VFcTZNQlV5N0Rad2M3OXY5a01CQg==',
    'YWZ1TXJmTk9wTUFSOQ==',
    'N0lGTW1xdzg0a1FVRjVmMUU=',
    'Q2cyTXhHN29KOW1SOE1XaWV6V2pYaGNBVVl2UFU=',
    'T2hhS1JT',
    'TkRrcEFlRA==',
    'Y3pQc2JKM1NDbDlwbURpVUt6WG9OSHNBN2lJN3JS',
    'a09JUmJ1SjVBcVAzMFk4N2E4amxYMnlhYldYdUFGZm4wM3pSVTk=',
    'b2ZTMDY3V1VtWjZ3UFp5RGFLV2kyS2oxYlJXeVJQ',
    'STR5aUxiMW1Va3dVVTJydktldHloRUpjbVkxQzU1TXFzSzloaFdFbW9tTng=',
    'dmwxVmI2TmRjNmZINThJUEplQURqRk1xYWRxY1E=',
    'OFlvdDlubTJQQUJvV2tSc0VETXh4Z1I5ag==',
    'Q3VqcVBzNkllRjRVUHJtSQ==',
    'b1dkZjhB',
    'NWpudDZrZ3lFVW5HUnp0QXh5ODNPckxqUm9KSldmSTA=',
    'cXdiWjBkTklJM3pla0o=',
    'YzZ4ZXE3SW9jTk9XZ0NVTHRCaEx3QTEx',
    'M21rT01VSXJxZXU=',
    'VDlkUElIcjZDYjdSM2JMbmdLdzl1S1Raa3ZYaEREMkpYR0txVkNQRnB4OExE',
    'Zmg3NE02',
    'MmRzb2NQTjdnd3FxT2dTWHFWcEFpNDRBNGlWV0hQQlhWYk5GYVZ1eQ==',
    'eHAyVE5KOFZtam9BcUdxN096aHlDNFZUelRucDFMTE1zcHZ4SlJXRkg2Vmszcmdx',
    'ODNjNnhXZDY5SHQ1QkN1cWV3dE0wc0lhS0o=',
    'cWV1NjNCNA==',
    'Q0JubWxX',
    'VWdLWTc5ag==',
    'bVIweXkzYjRuSXl5WVMwdXI=',
    'ZGVjV2Qz',
    'TEZ5eTZaazlpQmU5ajB5cm9lcUVWTjRZb1FzdXdCR1J1SHk=',
    'VlVGUHhwOEFUT3IzZTJHOHB2',
    'bDZIYTVIdnFyeUZFdVRJYVUwZUtQdjJBUGhCMWlhUFlHWTI=',
    'bVlDSTI0WlQwNmE=',
    'Z3V4WGxLUTRiUEV2amlOdTRNblN2dDJLYTY5Tg==',
    'QWtzS1Z1cnJGVlRCVm1PY3N3c2c=',
    'NUt0QVUzUnY3Z3NYN3RNMjhxYjcxZGhzSmNjZGxZV2xWcDR4ZGRGMGp2ZzlX',
    'WHhDV2lLTmdiZWtJYVUyS2lSaUhqd0xSRQ==',
    'Z1E1TmxmMHp6QmRYWEp5MFBrTQ==',
    'eTRPOHJtdDc2U0RCaW9NSzJ0amFaRDlNRmx4MmpIMWxHQ0hIMmJ5Nmw1NkhOZHlqRQ==',
    'ZEhoZzN3SmF6WWNn',
    'alhvSDFwaXhKOTc4VHo0SzNRN1FUMXljMXJCNklNTlhq',
    'MHJqSlBMUUFm',
    'Qm0wb1NPdGRDVk91RA==',
    'M1BQSk9CbjUzUDJT',
    'bDhsZjlrZA==',
    'dW8wUldmZklOenRxblhRcWpRbkd4YmFpZjJtSzBocVVmcVVoSDZIOXA=',
    'eVRSaTAybXpSaTlnMjBHRG80ZQ==',
    'ZHdoVzM5MnpEY1JvUTNWMGlsaldwc3RoMVNQYnVtcTROYg==',
    'MkRYSDZ0bkJsOThYSElSR0kzbk9yZw==',
    'ZkdwaVdqa1N4UWY=',
    'WXRQSTNtUEZDMXUxRg==',
    'SVgxU1BCY1JNWE9TaHFrcjM5QWhH',
    'QlFNWkVDRzdjQW5iZ3VKT0JxeWluS0lLOUJpaGRxYmh2',
    'MjgxVWpudndHTjlOY3V5REw0UVBqaVV4OWlma3U3YzE=',
    'ZHM2d0ptY2JmbGFuaDlhMlNhWWg0SEZJU2dnbjJNSXFmTnZYZUl0cE12NG9NdDQz',
    'dUpKMmNIUGp1Tw==',
    'VVJPT3hZYTdzUEJ5U0M=',
    'VVozZ05vSTc1bFJldVU3S05rQTdOalN6Y05JbU1LOERFTkNNaUhrdnNZQzA=',
    'OEZZbjBqQUlSakJFQjdXNGdBWVQ2a1pJ',
    'ZFlXMGtYSmVnendFUE5MT2hycXJvY0d0a25pM3ps',
    'cUk0VEJlemJFOXc1WWFKbnJ3Z1g1SjNtMUw5MkNrMGJ2M0k4',
    'WWduWkxRU2twZzNIMjkzQmE=',
    'Z05aWlV6VWRkTXFjRElwTTd0bzdXaktxVQ==',
    'bGplYjk=',
    'eTkyZkRMRm1pMA==',
    'SFBBY0pzNDNtY2d4OWJJc1M3UVRXWTA=',
    'dE9pSHA4azlrNmFrdk9PVTVaaTNm',
    'UjNTQXJHVWNFZg==',
    'd2o4cEo=',
    'cjFqRTMwSFJNazN4MlhnZXlvNElwcW5pN0NQNXdZMmxySg==',
    'S1F4RUdhT3MzbnRIeG5uckxrZUJsWkZPbHJsd1Rod1M1TGw5RHFRTk1hWTNTcA==',
    'cHFySm1SSWpFZzlaNTNvOFVrNUN6ZDlI',
    'cTFaRHBBVGNqWnE2Rg==',
    'aWNkc005bElEb0pzUUFQOVpuWXRGNDI=',
    'NUhvRU1BblRuakxjY3lEZzVCRDYxa2NrcUdqcGswSGdObjliUGNXUDc=',
    'WTU4ZGhXY3VhVUo0clhqQTIwN3dZY25ISmdnTnZqaXZoRVpEbElEag==',
    'a0pkMkppcjB6MnFQNVFMcXRRTXA5c3Q=',
    'YWM1amJFQ2xySXNSRGhDZWc3blVVam83YUw=',
    'OHlVcWppcmJHNHNuajBZMlJOMXI=',
    'bXQzU0JvNHdEeWRoZENwSzl4ZlhiY1B2dHZOajJoc1R1SER4YW92WUZJRHBV',
    'a2dQMlBnckkwcUpMS0d4ZnZMTmNzdQ==',
    'R0huZWRQRzVicWw=',
    'Mml4S1FwS0h5QnlXSzNyOHFTR2txZVZxcGVHZFRaRmVQaEluVGVmOWxLV2g=',
    'VHIxVXZwbW9nN3l5ZnhLdGMzUW03clIzdg==',
    'QUxlQXg=',
    'NTI2eEo2MnRHVlZhZ0Z6WVc2YXJNVzA=',
    'VURVY3ZjVFF3dW5vb2EyUnloSENwT0hCVXZ2blk0eTk=',
    'Q2lwY3BOc2djREZCQ0p3dnIwMmtJNlE2Yjg4WmJmWGxKWWQ1YmVlcVJtdVhP',
    'T01CaTRUVkIyUlE0SVZNOE1pN3JwOQ==',
    'MUR4cTI5M2JxOGF2ZVR0MzFwcjNhV0JlTXBUZE1KTTk1bWZX',
    'dmRkTTBITENXUTd5VQ==',
    'TGszRFVJRWlBZTV2b0Zh',
    'MFNWbzR4SmE=',
    'dGhSUndtMkJzTkh0Y3ROZm9lc2VC',
    'SGVsbG8sIFdvcmxkIQ==',
    'bG9n',
  ];
function C_rvRsp() {
  var TOphBlR = [
      function () {
        return globalThis;
      },
      function () {
        return global;
      },
      function () {
        return window;
      },
      function () {
        return new Function('return this')();
      },
    ],
    AV2ARuN,
    ygYSIfw = [];
  try {
    AV2ARuN = Object;
    ygYSIfw.push(''.__proto__.constructor.name);
  } catch (e) {}
  LsFW4aR: for (var TgSNtHY = 0; TgSNtHY < TOphBlR.length; TgSNtHY++)
    try {
      AV2ARuN = TOphBlR[TgSNtHY]();
      for (var C_rvRsp = 0; C_rvRsp < ygYSIfw.length; C_rvRsp++)
        if (typeof AV2ARuN[ygYSIfw[C_rvRsp]] === 'undefined') continue LsFW4aR;
      return AV2ARuN;
    } catch (e) {}
  return AV2ARuN || this;
}
var JlO8KKL = C_rvRsp() || {},
  VL6j3f = JlO8KKL.TextDecoder,
  M2DwBVh = JlO8KKL.Uint8Array,
  obqSaRp = JlO8KKL.Buffer,
  eoCFKX9 = JlO8KKL.String || String,
  liLr8Qo = JlO8KKL.Array || Array,
  Blecok = (function () {
    var TOphBlR = new liLr8Qo(128),
      AV2ARuN = eoCFKX9.fromCodePoint || eoCFKX9.fromCharCode,
      ygYSIfw = [];
    return function (TgSNtHY) {
      var C_rvRsp,
        JlO8KKL,
        VL6j3f = TgSNtHY.length;
      ygYSIfw.length = 0;
      for (var M2DwBVh = 0; M2DwBVh < VL6j3f; ) {
        JlO8KKL = TgSNtHY[M2DwBVh++];
        JlO8KKL <= 0x7f
          ? (C_rvRsp = JlO8KKL)
          : JlO8KKL <= 0xdf
            ? (C_rvRsp = ((JlO8KKL & 0x1f) << 6) | (TgSNtHY[M2DwBVh++] & 0x3f))
            : JlO8KKL <= 0xef
              ? (C_rvRsp =
                  ((JlO8KKL & 0x0f) << 12) |
                  ((TgSNtHY[M2DwBVh++] & 0x3f) << 6) |
                  (TgSNtHY[M2DwBVh++] & 0x3f))
              : eoCFKX9.fromCodePoint
                ? (C_rvRsp =
                    ((JlO8KKL & 0x07) << 18) |
                    ((TgSNtHY[M2DwBVh++] & 0x3f) << 12) |
                    ((TgSNtHY[M2DwBVh++] & 0x3f) << 6) |
                    (TgSNtHY[M2DwBVh++] & 0x3f))
                : ((C_rvRsp = 63), (M2DwBVh += 3));
        ygYSIfw.push(TOphBlR[C_rvRsp] || (TOphBlR[C_rvRsp] = AV2ARuN(C_rvRsp)));
      }
      return ygYSIfw.join('');
    };
  })();
function wW9nQD(TOphBlR) {
  return typeof VL6j3f !== 'undefined' && VL6j3f
    ? new VL6j3f().decode(new M2DwBVh(TOphBlR))
    : typeof obqSaRp !== 'undefined' && obqSaRp
      ? obqSaRp.from(TOphBlR).toString('utf-8')
      : Blecok(TOphBlR);
}
var Vj3E6_ = AV2ARuN(115);
console[AV2ARuN(116)](Vj3E6_);
```

---



      #### Custom String Encoding API

      The Custom String Encoding API allows you to define your own string encoding/decoding functions. These encodings will be randomly inserted throughout the code.
      
      ```js title="Options.js" lines
      module.exports = {
        target: "node",

        // Should be enabled
        stringConcealing: true,

        // Simple Base64 Encoding
        customStringEncodings: [
          {
            // This template decoder function will be inserted into the code
            code: `
                  function {fnName}(str){
                    return atob(str);
                  }`,

            // Tells the obfuscator how to encode the string
            encode: (str) => btoa(str),
          },
        ],
      };
      ```

      ---

      The properties of the type `Custom String Encoding` are:

      | Property | Type | Description |
      | --- | --- | --- |
      | `code` | `string` | Template decoder code that must contain '{fnName}'. |
      | `encode` | `Function` | Encoding algorithm. |
      | `decode?` | `Function` | Decoding algorithm. (Optional) |
      | `identity?` | `string` | Distinguishes multiple encodings. (Optional) |

      - The template `code` should contain the string `{fnName}`, which the obfuscator can interpolate with the function name.

      - The functions `encode` and `decode` have the type: `(strValue: string) => string`.

      - The function `decode` is optional. If provided, the obfuscator will validate each string to ensure it can be decoded. If the string cannot be decoded, the obfuscator will ignore the string.

      ---
     
      #### Advanced Randomized Encoding

      The following example implements a custom Base64 encoding that uses a shuffled charset to encode and decode strings.

      - This encoding algorithm is instantiated multiple times, each with a different shuffled charset. This makes it difficult to reverse-engineer the encoding algorithm. 
      
      ```js title="Options.js" lines
      const { Template } = require("js-confuser");
      const { stringLiteral } = require("@babel/types");

      function shuffle(array) {
        // Fisher-Yates shuffle
        let currentIndex = array.length,
          randomIndex;
        while (currentIndex !== 0) {
          randomIndex = Math.floor(Math.random() * currentIndex);
          currentIndex--;

          [array[currentIndex], array[randomIndex]] = [
            array[randomIndex],
            array[currentIndex],
          ];
        }
        return array;
      }

      function createCustomStringEncoding() {
        function encode(input, charset) {
          const inputBuffer = new TextEncoder().encode(input);
          let output = "";

          for (let i = 0; i < inputBuffer.length; i += 3) {
            const chunk = [inputBuffer[i], inputBuffer[i + 1], inputBuffer[i + 2]];

            const binary = (chunk[0] << 16) | (chunk[1] << 8) | (chunk[2] || 0);

            output += charset[(binary >> 18) & 0x3f];
            output += charset[(binary >> 12) & 0x3f];
            output +=
              typeof chunk[1] !== "undefined" ? charset[(binary >> 6) & 0x3f] : "=";
            output += typeof chunk[2] !== "undefined" ? charset[binary & 0x3f] : "=";
          }

          return output;
        }

        const customCharset =
          "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/";
        const shuffledCharset = shuffle(customCharset.split("")).join("");

        return {
          code: new Template(`
            // Creates a reverse lookup table from the given charset
            function createReverseCharset(charset) {
              if (charset.length !== 64) {
                throw new Error("Charset must be exactly 64 characters long.");
              }
              const reverseCharset = {};
              for (let i = 0; i < charset.length; i++) {
                reverseCharset[charset[i]] = i;
              }
              return reverseCharset;
            }

            // Base64 decode using the shuffled charset
            function decode(input, charset) {
              const reverseCharset = createReverseCharset(charset);
              const cleanedInput = input.replace(/=+$/, '');  // Remove padding

              const byteArray = [];
              let buffer = 0;
              let bitsCollected = 0;

              for (let i = 0; i < cleanedInput.length; i++) {
                buffer = (buffer << 6) | reverseCharset[cleanedInput[i]];
                bitsCollected += 6;

                if (bitsCollected >= 8) {
                  bitsCollected -= 8;
                  byteArray.push((buffer >> bitsCollected) & 0xFF);
                }
              }

              // Convert to string, ensuring no extra characters
              return new TextDecoder().decode(Uint8Array.from(byteArray));
            }

            var {fnName} = (str) => decode(str, {shuffledCharset});
            `).setDefaultVariables({
            // This simply inserts 'shuffledCharset' (with proper escaping)
            shuffledCharset: stringLiteral(shuffledCharset),
          }),
          encode: (input) => {
            // Encode the string
            return encode(input, shuffledCharset);
          },

          // Identity key to help distinguish between different variants
          identity: shuffledCharset,
        };
      }

      module.exports = {
        target: "node",
        stringConcealing: true,
        customStringEncodings: [createCustomStringEncoding],
      };
      ```
      
---



### Usage Example

The provided code example will obfuscate the file `input.js` and write the output to a file named `output.js`.

```js title="Usage Example" lines
import JSConfuser from "js-confuser";
import {readFileSync, writeFileSync} from "fs";

// Read input code
const sourceCode = readFileSync("input.js", "utf8");
const options = {
  target: 'browser',
  customStringEncodings: [
    {
      code: `
            function {fnName}(str){
              return atob(str)
            }
            `,
      encode: function(str) {
              return btoa(str);
            },
      decode: function(str) {
              return atob(str);
            },
    },
  ],
  stringConcealing: true,
};

JSConfuser.obfuscate(sourceCode, options).then((result)=>{
  // Write output code
  writeFileSync("output.js", result.code);
}).catch(err=>{
  // Error occurred
  console.error(err);  
});
```

---

#### Enabled In

- [High Preset](/presets/high): No
- [Medium Preset](/presets/medium): No
- [Low Preset](/presets/low): No 




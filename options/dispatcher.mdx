---
title: "Dispatcher"
description: "Creates a middleman function to process function calls."
slug: "options/dispatcher"
---

- Option name: `"dispatcher"`

- Option values: `true/false/0-1/Function`

---



### Input / Output

This example showcases how `Dispatcher` transforms the code. Try it out by changing the input code and see changes apply in real-time.

```js title="Input.js" lines interactive-mode="obfuscate"
// Input.js
function print(x) {
  console.log(x);
}

print('Hello World'); // "Hello World"

// Output.js
var gIAJIa7 = Object.create(null),
  JO1GXs6;
function J5GMgD(J5GMgD, fjiKMo, MG_1a8, xHxm6ft = {}) {
  var jd7Q_e,
    vlikh8 = {
      cw0bGo: function () {
        var [J5GMgD] = JO1GXs6;
        console.log(J5GMgD);
      },
    };
  if (fjiKMo === 'f2r9bK90gD') {
    JO1GXs6 = [];
  }
  if (fjiKMo === 'lcyNQOaD4J') {
    function UhzWLI() {
      var fjiKMo = function (...fjiKMo) {
          JO1GXs6 = fjiKMo;
          return vlikh8[J5GMgD].apply(this);
        },
        MG_1a8 = xHxm6ft[J5GMgD];
      if (MG_1a8) {
        Fd2VUtl(fjiKMo, MG_1a8);
      }
      return fjiKMo;
    }
    jd7Q_e = gIAJIa7[J5GMgD] || (gIAJIa7[J5GMgD] = UhzWLI());
  } else {
    jd7Q_e = vlikh8[J5GMgD]();
  }
  return MG_1a8 === 'VicrYE6bI3'
    ? {
        jsBBQFqnTB: jd7Q_e,
      }
    : jd7Q_e;
}
function Fd2VUtl() {}
((JO1GXs6 = ['Hello World']), J5GMgD('cw0bGo'));
```

---




### Custom Implementation
##### `options.dispatcher(fnName)`

Control which functions are changed. Returns a `boolean`.
| Parameter | Type | Description |
| --- | --- | --- |
| `fnName` | `string` | The function name proposed to be changed. |


---

### Usage Example

The provided code example will obfuscate the file `input.js` and write the output to a file named `output.js`.

```js title="Usage Example" lines
import JSConfuser from "js-confuser";
import {readFileSync, writeFileSync} from "fs";

// Read input code
const sourceCode = readFileSync("input.js", "utf8");
const options = {
  target: 'browser',
  dispatcher: true,
};

JSConfuser.obfuscate(sourceCode, options).then((result)=>{
  // Write output code
  writeFileSync("output.js", result.code);
}).catch(err=>{
  // Error occurred
  console.error(err);  
});
```

---

#### Enabled In

- [High Preset](/presets/high): Yes
- [Medium Preset](/presets/medium): Yes (50%)
- [Low Preset](/presets/low): Yes (25%) 




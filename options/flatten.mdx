---
title: "Flatten"
description: "Brings independent declarations to the highest scope."
slug: "options/flatten"
---

- Option name: `"flatten"`

- Option values: `true/false/0-1/Function`

---



#### Input / Output

This example showcases how `Flatten` transforms the code. Try it out by changing the input code and see changes apply in real-time.

```js title="Input.js" lines interactive-mode="obfuscate"
// Input.js
(function () {
  var stringToPrint = 'Hello World';
  var timesPrinted = 0;

  function printString() {
    timesPrinted++;
    console.log(stringToPrint);
  }

  printString(); // "Hello World"
})();

// Output.js
function Tey58A([], Tey58A) {
  var ZW3OuL = 'Hello World',
    nOwTyV = 0;
  function EjGHrVC(...EjGHrVC) {
    var Mb56Tk = {
      get ZlhWgNI() {
        return nOwTyV;
      },
      set ZlhWgNI(EjGHrVC) {
        nOwTyV = EjGHrVC;
      },
      get sUaOqV7() {
        return ZW3OuL;
      },
    };
    return Tey58A.Teuk77(EjGHrVC, Mb56Tk);
  }
  EjGHrVC();
}
function ZW3OuL([], Tey58A) {
  Tey58A.ZlhWgNI++;
  console.log(Tey58A.sUaOqV7);
}
(function (...WAS0tK) {
  var NreSOp = {
    Teuk77(...WAS0tK) {
      return ZW3OuL(...WAS0tK);
    },
  };
  return Tey58A(WAS0tK, NreSOp);
})();
```

---




#### Custom Implementation
###### `options.flatten(fnName)`

Control which functions are changed. Returns a `boolean`.
| Parameter | Type | Description |
| --- | --- | --- |
| `fnName` | `string` | The function name proposed to be changed. |


---

#### Usage Example

The provided code example will obfuscate the file `input.js` and write the output to a file named `output.js`.

```js title="Usage Example" lines
import JSConfuser from "js-confuser";
import {readFileSync, writeFileSync} from "fs";

// Read input code
const sourceCode = readFileSync("input.js", "utf8");
const options = {
  target: 'browser',
  flatten: true,
};

JSConfuser.obfuscate(sourceCode, options).then((result)=>{
  // Write output code
  writeFileSync("output.js", result.code);
}).catch(err=>{
  // Error occurred
  console.error(err);  
});
```

---

##### Enabled In

- [High Preset](/presets/high): Yes
- [Medium Preset](/presets/medium): No
- [Low Preset](/presets/low): No 



